import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#A8E3BD",
    },
    secondary: {
      main: "#D6F5F4",
    },
  },
  components: {},
});

export default theme;
